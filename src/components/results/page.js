import React, { Fragment } from 'react';

const Page = (props) =>{
    // console.log(props.results)
    // const { results }= props;
    return (
        <Fragment>
            
            {/* {results ?
                results.map(pokemon => (
                    // bootstrap Card
                ));
                :
                "None"
            } */}
        </Fragment>
    );
}

export default Page;